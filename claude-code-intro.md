# Claude Code とは何か？ ── AIと一緒にコードを書く、新しい開発体験

> 「ターミナルに住む AI パートナー」── そう表現すると、Claude Code の本質が一番伝わります。

---

## はじめに

「GitHub Copilot は使ったことあるけど、Claude Code って何が違うの？」

そう思っている方に向けて、この記事では **Claude Code をできるだけやさしく解説** します。専門用語は最小限にして、「何ができて、何が嬉しいのか」を中心に説明します。

---

## Claude Code とは？

**Claude Code** は、Anthropic（アンソロピック）社が開発した **ターミナル（コマンドライン）で動く AI コーディングツール** です。

2025年2月にプレビュー版が公開され、同年5月に正式リリース。現在（2026年）も急速に進化しています。

### 一言で言うと

```
あなたのコードベースを理解して、自然言語で指示するだけで
ファイル編集・テスト実行・Git 操作まで「自律的に」やってくれる AI アシスタント
```

---

## 他の AI コーディングツールとの違い

| ツール | どこで動く？ | 主な強み |
|--------|------------|---------|
| **GitHub Copilot** | VS Code 等の拡張機能 | コード補完・スニペット生成が得意 |
| **Cursor** | AI 搭載エディタ（VS Code ベース） | ファイルをまたいだリファクタリングが得意 |
| **Claude Code** | **ターミナル（CLI）** | **複雑な問題の推論・プロジェクト全体の把握** |

Copilot や Cursor が「入力候補を出してくれる賢いオートコンプリート」だとすると、Claude Code は **「プロジェクトの全体像を理解したうえで、複数ステップのタスクを自律的にこなすエージェント」** です。

---

## Claude Code でできること

### 1. 自然言語でコードを読み書きする

```bash
$ claude

> この関数のバグを直して、テストも追加して
> 認証まわりのコードを説明して
> API エンドポイントを REST から GraphQL に変更して
```

ファイルの中身を理解した上で、修正・説明・リファクタリングを行います。

### 2. テストの実行・デバッグ

「テストを実行して、失敗したら原因を特定して直して」──この一言で、テスト実行から原因調査、コード修正まで自律的に進めます。

### 3. Git 操作

```bash
> 変更内容をまとめた commit メッセージを書いて、コミットして
> 今のブランチと main の差分を説明して
> PR の説明文を書いて
```

### 4. コードベース全体の把握

200,000 トークンという巨大なコンテキストウィンドウを持つため、大規模プロジェクトでも全体を把握した上で回答・修正できます。小さなチャット欄に貼り付けてやり取りする必要がありません。

### 5. 並列エージェント（サブエージェント）

複数の調査や作業を **並列で** 実行できます。「A と B を同時に調べて」「フロントエンドとバックエンドのテストを同時に実行して」といった使い方が可能です。

---

## 特徴的な機能

### CLAUDE.md ── プロジェクトの「取扱説明書」

プロジェクトのルートに `CLAUDE.md` というファイルを置くと、Claude Code が毎回それを読み込みます。

```markdown
# CLAUDE.md の例

## プロジェクト概要
ECサイトのバックエンド API（Python / FastAPI）

## コーディング規約
- 変数名はスネークケース
- 関数には必ず型ヒントを書く
- テストは pytest を使う

## よく使うコマンド
- テスト実行: pytest -v
- サーバー起動: uvicorn main:app --reload
```

これにより「毎回同じことを説明しなくてよくなる」のが大きな利点です。

### Hooks ── 自動化の仕組み

ファイルを編集したとき・コマンドを実行する前後など、特定のイベントに合わせてシェルスクリプトを自動実行できます。

例：「Claude がファイルを編集するたびに自動でリンターを走らせる」

### MCP（Model Context Protocol）

外部サービス（データベース、Slack、GitHub など）と Claude Code を接続して、より広い操作を自然言語で行えます。

---

## 使ってみると何が嬉しい？

### ケース①：「このバグの原因を調べたい」

従来：
1. エラーログを見る
2. 関連ファイルを手動で開いて読む
3. Stack Overflow で調べる
4. 試行錯誤して直す

Claude Code なら：
```
> このエラーの原因を調べて、直して
```
コードベースを横断的に調べ、原因を特定し、修正案を提示（または自動で修正）してくれます。

### ケース②：「初めてのコードベースを理解したい」

```
> このプロジェクトの全体構成を説明して
> 認証フローはどこで処理されている？
> このファイルが何をしているか教えて
```

入社したばかりの大規模プロジェクトや、OSS のコードを読むときに特に強力です。

---

## 始め方

### インストール

```bash
npm install -g @anthropic-ai/claude-code
```

Node.js がインストールされていれば、このコマンドだけで使えます。

### 起動

```bash
cd あなたのプロジェクト
claude
```

あとは日本語で話しかけるだけです。

### 料金（2026年3月時点）

| プラン | 月額 | Claude Code |
|--------|------|-------------|
| Free | 無料 | ❌ 利用不可 |
| Pro | $20 / 月 | ✅ 利用可能 |
| Max 5x | $100 / 月 | ✅ 高頻度利用向け |
| Max 20x | $200 / 月 | ✅ ヘビーユーザー向け |

---

## 向いている人・向いていない人

### こんな人に向いている

- 大きなコードベースを扱う開発者
- バグの原因調査や説明に時間がかかっていると感じる人
- 繰り返しの作業（テスト追加、コメント記述、リファクタリングなど）を減らしたい人
- ターミナルを普段使いしている人

### こんな人には他のツールが向いているかも

- とにかくコード補完が欲しいだけ → GitHub Copilot で十分
- GUI で完結させたい → Cursor が使いやすい
- IDE から離れたくない → IDE 統合ツールを検討

---

## まとめ

| 観点 | Claude Code |
|------|------------|
| **動作場所** | ターミナル（CLI） |
| **得意なこと** | 複雑な推論・プロジェクト全体の把握・自律的なタスク実行 |
| **コンテキスト** | 最大 200,000 トークン（大規模プロジェクトでも対応） |
| **特徴的な機能** | CLAUDE.md / Hooks / MCP / サブエージェント |
| **料金** | Claude Pro（$20/月）から利用可能 |

Claude Code は単なる「コード補完ツール」ではなく、**プロジェクトを理解して自律的に動けるエージェント型の AI アシスタント** です。

「指示を出すと、後はやっておいてくれる」── その感覚を一度体験すると、開発のやり方が根本から変わるかもしれません。

---

## 参考リンク

- [Claude Code 公式ドキュメント](https://code.claude.com/docs/en/overview)
- [Claude Code GitHub リポジトリ](https://github.com/anthropics/claude-code)
- [Claude 料金プラン](https://claude.ai/upgrade)
